# drivercom-gui

## Development

### Requirements

1. `pnpm`
2. `rustc` & `cargo`

### Setup

`pnpm i`

`pnpm exec prettier --write .`

`tsc`

### Running

To run a development build, use `pnpm run tauri dev`
